'use strict';

require('../__tests-server__/test-config');
// const { mount, requireUser } = require('../src/init');

describe('init', () => {

  it.skip('should be something or other', () => {
    // some ting wong
  });

});
